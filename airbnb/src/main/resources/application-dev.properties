# Local development database
spring.datasource.url=jdbc:postgresql://localhost:5432/airbnb_database

# In dev, let Hibernate auto-update the schema for fast iteration
spring.jpa.hibernate.ddl-auto=update

# Flyway disabled in dev so Hibernate can freely manage schema while you build
spring.flyway.enabled=false